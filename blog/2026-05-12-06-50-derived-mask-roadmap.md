# Derived Mask Roadmap

All tickets through `TICKET-0044` are complete. The next suite focuses on the
primitive gap left deliberately open by the monolithic train step: deriving the
ReLU mask inside LeanAX IR.

`TICKET-0045` through `TICKET-0049` add compare/select support, a derived ReLU
mask artifact, a derived-mask MNIST train step, validation failures for the new
predicate/select boundaries, and a readiness-report closeout.
